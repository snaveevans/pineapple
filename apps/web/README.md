# apps/web

This directory is reserved for the future FieldOps frontend.

The intended shape is a separate Cloudflare-hosted web application that consumes the API from `apps/api`.

No frontend toolchain or source code is added in Phase 0.
