# packages

This directory is reserved for shared code that has more than one real consumer.

Expected future uses include:

- shared API contracts
- shared validation schemas
- shared utility types

No shared package is created in Phase 0.
