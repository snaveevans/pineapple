dist/
node_modules/
pnpm-lock.yaml

# Generated from code by `openapi:generate`; formatting must match the script's
# output so the CI drift check stays stable. Do not let Prettier reformat it.
docs/reference/openapi.json

# Generated by `wrangler types` (cf-typegen); formatting must match wrangler's
# output so the CI drift check stays stable. Do not let Prettier reformat it.
apps/api/worker-configuration.d.ts

# Generated from openapi.json by `api:types`; formatting must match the
# generator's output so the CI drift check stays stable (issue #117).
apps/web/src/api/schema.ts
